# Database URL (SQLite by default, change to PostgreSQL for production)
DATABASE_URL=sqlite:///./internet_usage.db

# For PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/internet_usage
